<UserControl x:Class="Ymm4TachieAnimationPlugin.Editor.Controls.FileSelectorWithRigEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="32" d:DesignWidth="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBox x:Name="PathTextBox" 
                 Grid.Column="0" 
                 VerticalContentAlignment="Center" 
                 Padding="4,2"
                 Text="{Binding Path=Value, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

        <Button Grid.Column="1" 
                Content="📄" 
                Width="28" Margin="4,0,0,0" 
                ToolTip="PSDファイルを選択" 
                Click="OnBrowseFileClicked" />

        <Button Grid.Column="2" 
                Content="🛠️ ボーン編集" 
                Padding="6,2" Margin="4,0,0,0" 
                ToolTip="2Dボーンリグエディターを起動してボーン構造やアニメーションを編集します" 
                Click="OnOpenRigEditorClicked" />
    </Grid>
</UserControl>
